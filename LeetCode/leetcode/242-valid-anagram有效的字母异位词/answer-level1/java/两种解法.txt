先判断两个字符串的长度是否相等，如果不相等就直接返回false
之后的解法可以有两种
1、比较两个字符串中字符的个数，创建一个128位的int 数组，遍历统计字符串中字符出现的个数
```
int[] nums = new int[128];
for (int i = 0; i < s.length(); i++) {
    nums[s.charAt(i)] += 1;
    nums[t.charAt(i)] -= 1;
}
```
然后判断nums数组是否都为0即可

2、解法可以先把两个字符串进行排序，然后直接比较两个字符串是否相等即可