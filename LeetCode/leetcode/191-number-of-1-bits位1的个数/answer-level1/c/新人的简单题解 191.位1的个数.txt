### 解题思路
有/无符号整数存储方式是一样的，只是无符号整数只能存非负数（相当于把有符号整数的负数部分用作正数）。
计算机中存储数值用的是补码，但是正数原码和补码在形式上是一样的，所以不用考虑太多，当作一般二进制问题用位运算解决就好了。

### 代码

```c
int hammingWeight(uint32_t n)
{
     int temp=0;
     for(int i=0;i<32;i++)
     {
        temp+=n&1;
        n>>=1;
     }
    return temp;
}
```