### 解题思路
将它按成整数来搞，借助计算机理论基础：

`把一个整数减去1，再和原来的整数做相与运算，会把该整数二进制的最右边的1变成0`

### 代码

```c
int hammingWeight(uint32_t n) {
    int c = 0;
    while(n != 0){
        c++;
        n = n & (n - 1);
    }
    return c;
}
```