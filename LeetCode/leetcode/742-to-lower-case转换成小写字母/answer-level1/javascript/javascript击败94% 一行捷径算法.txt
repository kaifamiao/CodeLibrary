```
/**
 * @param {string} str
 * @return {string}
 */
var toLowerCase = function(str) {
  return str.toLocaleLowerCase()
};
```
对js 常用方法了解的话一条，当然题目本意不是直接走捷径 后续再加一个实现算法。
见下方介绍
[toLocaleLowerCase](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/String/toLocaleLowerCase)