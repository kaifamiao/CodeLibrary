偷偷说一句，其实你把官方C++答案直接拷贝到Java做答案，也能通过

其实和官方的思路一样的，由于两个数组已经做过排序，并且A数组后半部分留有B数组的位置

其实只要对比A数组最后位和B数组的最后位，然后将其中大的的数据放到A数组最后位置，然数字比较大的数组指针往前移动一位，对比下一个数是否大于之前对比数，然后谁的数大就放入后半部分，然后指针前移
else if(A[Asize]>B[Bsize]){
 cur=A[Asize--];
}else{
 cur=B[Bsize--];
}
对比末尾的数字，并且指标移动
当其中一个数组对比完了，则直接将剩余的数组按照顺序放入A数组
if(Asize==-1){
 cur=B[Bsize--];
}else if(Bsize==-1){
cur=A[Asize--];
 }
