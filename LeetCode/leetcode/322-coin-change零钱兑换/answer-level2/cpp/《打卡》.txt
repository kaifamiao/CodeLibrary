```
ass Solution {public:    int coinChange(vector<int>& coins, int amount)    {        int dp[amount+1] = {0};        for(int i=0; i < coins.size(); i++){            if(coins[i] > amount)                continue;                        dp[coins[i]] = 1;                }                for(int i = 0; i <= amount; i++){                        for(int j = 0; j < coins.size(); j++){                     if(coins[j] >= i)                    continue;                else if(dp[i-coins[j]]!=0){                    if(dp[i] == 0)                        dp[i] = dp[i - coins[j]] + 1;                    else                        dp[i] = dp[i-coins[j]]+1<dp[i]?dp[i-coins[j]]+1:dp[i];                }                               }               }        if(amount==0)           return 0;        return dp[amount] == 0? -1: dp[amount];        }};>}}>)}>)>)}}
```
