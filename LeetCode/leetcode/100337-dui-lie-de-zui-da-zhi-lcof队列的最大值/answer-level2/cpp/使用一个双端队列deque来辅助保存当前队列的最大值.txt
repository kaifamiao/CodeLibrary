queue data用于保存入队的所有值；deque max_data用于保存当前队列的最大值：
插入一个元素时：如果max_data中从队尾开始存在比该元素值小的元素，则移出队列pop_back()，指导队尾值大于该元素或者max_data为空时，才将该元素添加到max_data队尾；
删除队头元素时：判断队头元素是否等于max_data的队头元素，如果相等，则同时将，max_data队头出队pop_front();
之所以使用双端队列，是因为双端队列可以两头插入和删除。