    时间复杂度为O(2^n)
    空间复杂度为O(1)
    
    斐波那契数：当前数为前两数之和且当数个数大于2的情况。
    例子：0 1 1 2 3 5 8.......
    思路：f(n) = f(n-1) + f(n-2),也就是我们可以把一个大的问题转换为相同方式下小的方式.每一个小问题继续往下拆解,直到转换为常数问题即可.
    解：
    public int fib(int N) {
        if (N <= 1) return N;
        return fib(N - 1) + fib(N - 2);
    }