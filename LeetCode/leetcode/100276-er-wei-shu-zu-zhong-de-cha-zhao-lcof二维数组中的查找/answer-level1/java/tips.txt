` if (matrix==null||matrix.length==0||matrix[0].length==0) return false;`
写出
` if (matrix==null||matrix[0].length==0||matrix.length==0) return false;`
会报错，因为你既然matrix都没有元素了，哪里来的matrix[0]
思路不清晰的看，懂得人孝一孝就好，不要喷，我是你哥哥