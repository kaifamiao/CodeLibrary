上一题，看了双指针解法的删除排序数组中的重复项题解，通过交换的方式不用删除元素和插入可以提高运行速率。
思路基本与#26题一致，如下：
- 前指针index，后指针len - 1，向后遍历；
- 对于不相等的元素不操作；
- 相等则用末尾元素替换，替换后该位置为新元素，遍历指针不向后移index--，末尾元素因为换过来了，不需要再检查也不能用于交换len--;
- 往下遍历，直到重复元素被替换完，最后返回排查重复元素后的len