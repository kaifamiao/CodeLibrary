该题实际上就是传递到最远点的时间耗时，因此可以认为是从起始S到某个节点的距离，最长的耗时就是距离起始点最远的节点的最小距离就是所有节点都收到信号的时间点。该问题的前提是所有的节点都能从当前起始节点访问，否则信号不会传递到对应的节点。因此变成了从当前节点开始的最短路径问题。

由于没有负数边的影响，因此可以尝试Dijkstra算法计算出当前S节点到其他节点的最短路径，然后找到最大的最短路径即可找到最大的网络延迟时间。

因此首先涉及到从数组转换成图的过程，创建图，有向边的管理等。最终通过Dijkstra算法的实现。
