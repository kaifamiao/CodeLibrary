SELECT  B.SCORE,A.RN RANK
  FROM (SELECT ROWNUM RN, SCORE
          FROM (SELECT DISTINCT (SCORE) FROM SCORES ORDER BY SCORE DESC)) A,
       SCORES B
 WHERE A.SCORE = B.SCORE
 ORDER BY RANK;

利用rownum建立一张新的表 已经去重复的表 再和原表进行关联就可以了