```
bool divisorGame(int N){
    // 如果a拿到奇数，则必输，
    // 因为奇数的因数必为奇数，则N-x必为偶数，
    // b拿到偶数之后，只需要-1交还给a，则a再次拿到奇数，
    // 这样b永远拿到偶数，直到拿到2赢下比赛

    // 反过来，如果a拿到偶数，只需要-1给b，那么b就必输。
    return N%2==0;
}
```