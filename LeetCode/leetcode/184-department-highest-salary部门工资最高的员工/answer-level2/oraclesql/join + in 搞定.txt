select d.name department,e.name employee,e.salary from employee e  join department d on e.departmentid=d.id where (e.salary,e.departmentid) in (select max(salary),departmentid from employee group by departmentid);