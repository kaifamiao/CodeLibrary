### 解题思路
递归，&&的使用，&&还真没想到，只知道肯定要递归，&&前面的不符合后面的就中断。。

### 代码

```cpp
class Solution {
public:  //写递归题目就要知道当前节点需要干什么，其余的交给递归
    int sumNums(int n) {
        n&&(n+=sumNums(n-1)); //不符合就返回0，符合就给n重新赋值。。
        return n;
    }
};
```