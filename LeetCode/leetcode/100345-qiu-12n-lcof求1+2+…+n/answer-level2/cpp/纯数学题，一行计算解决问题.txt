### 解题思路

利用小学学过的求和方式，两边收缩相加。
不能使用条件判断，难点是解决奇偶数问题，利用 奇数%2==1这个特点，处理中间的数字

### 算法
(1+n) * (n/2) + (n%2)*(n/2+1);

### 代码

```cpp
class Solution {
public:
    int sumNums(int n) {
        //数学题
        return (1+n) * (n/2) + (n%2)*(n/2+1);
    }
};
```