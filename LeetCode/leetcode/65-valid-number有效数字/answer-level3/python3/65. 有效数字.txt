### 解题思路
这类题字符串匹配题最一劳永逸的方法就是用正则表达式，而正则表达式是由编译原理里的自动机实现的。。。下面是借鉴一位大佬的代码

### 代码

```python3
class Solution:
    def isNumber(self, s: str) -> bool:
        # 用正则表达式解决字符串匹配问题
        return bool(re.match(r' *[+-]?([0-9]+(\.[0-9]*)?|\.[0-9]+)(e[+-]?[0-9]+)? *$', s))
```