 public boolean isPowerOfTwo(int n) {
        //负数和小于0的情况不考虑
        if (n == 0 || n < 0) {
            return false;
        }
       //利用了2进制的特征，若是2的n次方法，则该值一定是最高位为1，后面位都是0，
        //例如4，2进制：100;8,2进制为1000，那么他们-1，后面位必为1，两者相&必为0
        return (n & n - 1) == 0;
    }