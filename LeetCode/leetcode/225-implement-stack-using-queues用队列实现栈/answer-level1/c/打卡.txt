1. 首先栈的pop等于pop出队尾。
2. 其次栈的push等于push到队尾。
3. 由于只能使用基本操作，那么pop的时候将整个队列pop出的同时push进直到将原队尾的元素pop出即可,因此使用链表实现的队列。

## MyStack
##  push操作
时间复杂度:O(1)
空间复杂度:O(1)
## pop操作
时间复杂度:O(n)
空间复杂度:O(1)
## top操作
时间复杂度:O(n)

刚学c不知道是不是这个样子，我理解的只能用队列的push(),pop(),size(),is_empty()实现的话。