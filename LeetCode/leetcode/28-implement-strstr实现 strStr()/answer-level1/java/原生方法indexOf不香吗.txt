执行用时：0 ms，内存消耗：38 MB
```
        if (haystack == null) {
            return -1;
        } else if (needle.equals("")) {
            return 0;
        } else {
            return haystack.indexOf(needle);
        }
```
