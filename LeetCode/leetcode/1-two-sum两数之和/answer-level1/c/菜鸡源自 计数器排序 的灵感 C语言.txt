可以尝试使用计数器的方法，创建一个数组（开辟一块内存，先开 辟一块 第一个数+1 大小的指针型数组，如果遇到空间不够就在补。），将所有数的地址存入这个数相对应的下标的数组里（例：如果这个数是5，就将其指针存入a[5]，如果这个数不存在就记作NULL）。接着再遍历这个数组如果a[target - i]不为NULL的话，就意味着找到了相匹配的数，直接返回就行了。这样子时间复杂度就是线性的，当然如果数太大的话程序也会比较慢。不过只要最大值不是大得离谱就还好。