创建一个可变的list集合保存索引。用for循环遍历nums数组，创建一个another保存另一个整数，判断数组是否存在这一个整数。如果存在，则将该索引的值赋一个Int的最大值，再次判断数组是否存在another，防止another是target本身，两次判断都存在就往list中添加对应的索引并将另一个索引对应的值也改为最大值，让循环到该索引时可以直接不通过判断。
存在缺陷，比如修改的值刚好可以成为另一个数的整数可能。