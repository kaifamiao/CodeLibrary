此问题是给定无序的数组，求数组中三元素和为0的所有组合（需去重）

方法一：
三数求和为0， 则存在的组合为（负、负、正），（负，正，正）两种模式
那第一步是先采取快排的第一种方式，经过一次遍历把数值调整为前部分为负数，后部分为正数。
第二部确定数组中第一个正数的索引pos_index（这个很有用）
第二步就是遍历，第一个指针从头到pos_index遍历，第三个指针从尾到pos_index遍历，第二个指针在第一和第三个指针中间遍历，
判断三元素之和是否为0. 

此方法能找出组合，但是无法做到排重，时间复杂度是O(n^3)

方法二：明天再补充吧