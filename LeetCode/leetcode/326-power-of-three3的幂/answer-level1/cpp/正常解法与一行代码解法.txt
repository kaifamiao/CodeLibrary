LeetCode 326 3的幂
给定一个整数，写一个函数来判断它是否是 3 的幂次方。

一行代码解法：
在讨论区学习到，假如n为3的幂，那么n一定可以被32位机整数整数范围内3的最大次幂（3^19==1162261467）整除
代码实现：
class Solution {
public:
    bool isPowerOfThree(int n) {
        return n <= 0 ? 0 : ( 1162261467 % n ) == 0 ;
    }
};

正常解法就是循环，不再赘述。