使用动态规划方法：假如`p(i,j)`是一个回文子串，且它表示取自字符串s从位置i到j的子字符串，当`i = j`时为单个字符，那么如果当`s[i-1] == s[j+1]`时，表示·p(i-1,j+1)·也是回文子串，相应长度增加。这样把字符串从前往后遍历一遍即可找到回文子串。其中需要注意的是有的回文子串并不是从单个字符开始，而是从相同的两个字符开始，因为从单个字符按照上面方法去搜寻，最终的回文子串长度只可能是奇数，而偶数长度的回文子串都是从相等的两个字符开始的，故考虑了两种情形就可以搜寻任意长度的回文子串了。