我们可以想怎么能不舍弃大的值：
    由于min()最大值肯定是被舍弃了，那么我们可以想怎么去选择保留次大值,肯定是将最大值与次大值组成一队。
    那么从原数组中排除这两个元素之后，剩下的元素又是跟上一步同样的问题。
    由此 我们可以将数组排序，这样两个连续的数组成一组就是题目要求的组队，而每组的前一个数就是要进行加和的数。所以就是将排序后的奇数下标的值加起来。

除了思想，这里主要考察的就是排序算法，自己写的插入排序超时，只能嫖sort函数了