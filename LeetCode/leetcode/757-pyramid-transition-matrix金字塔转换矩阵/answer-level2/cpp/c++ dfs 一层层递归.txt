首先是要知道一层层递归的意义.
1. 就是当前层都得出结果了, 那么下一次递归就要从这个新的递归结果开始
2. 如果当前层还没有处理完,那么下一次递归仍然是处理当前层的下一对字符
所以递归函数里面就要有两处递归的调用,参数肯定是不一样的,
1. 用新的结果的递归dfs(1, old_string, "");
2. 当前层还没有处理完dfs(index + 1, old_string, result__string);
递归的出口,什么时候递归结束呢?
当前层的大小为1,直接得出结果了
下一层的大小比当前层大小小1,这时可以认为当前层处理完了.

